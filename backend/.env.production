# Production environment variables for Railway deployment
NODE_ENV=production
PORT=3001

# Frontend URL for CORS
FRONTEND_URL=https://graceful-optimism-production.up.railway.app

# Database configuration (Railway will provide DATABASE_URL)
# For SQLite in production (not recommended for multi-device sync)
# Consider using Railway's Postgres instead